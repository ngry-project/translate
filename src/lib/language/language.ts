
/**
 * Type alias for language code.
 * Language code can be any string.
 *
 * @example
 * ```ts
 * const ua: LanguageCode = 'ua';
 * const en: Array<LanguageCode> = ['en-GB', 'en-US'];
 * ```
 * @since 2.0.0
 */
export type Language = string;
