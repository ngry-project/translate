
/**
 * Represents source of operator that compares for equality.
 * @since 2.0.0
 */
export type EqualOperatorData = {
  /**
   * Gets value to which compare to.
   * @since 2.0.0
   */
  readonly $eq: string | number | boolean;
};
